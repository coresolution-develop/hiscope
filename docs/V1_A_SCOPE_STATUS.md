# v1.0 A Scope Status (Final Check)

기준일: 2026-03-12  
대상: 다면평가 시스템 v1.0 필수 범위(A)

## 1. 현재 구현 현황 요약

- A 범위 핵심 기능은 운영 가능 수준으로 대부분 구현 완료.
- 최근 배치에서 목록형 화면의 검색/필터/정렬/페이징 일관성을 크게 보강함.
- 권한/기관 격리, 업로드 실패 추적, 감사 로그 기본형은 시나리오 테스트로 반복 검증 중.

## 2. 도메인별 기능 인벤토리

### 2.1 최고 관리자 기능

#### 이미 구현됨
- 기관 생성/수정/활성/비활성
- 기관 목록 조회 + 검색/상태필터 + 정렬/페이징
- 기관 관리자 생성/수정/상태변경/비밀번호 초기화
- 기관별 운영 요약(사용자 수/세션 수/진행률/최근 업로드/최근 오류)

#### 보완 필요
- 기관 운영 요약 항목의 추가 통계(고급 지표)는 B 또는 이후 범위

#### 미구현
- 없음(A 기준)

### 2.2 기관 관리자 기능 (부서/사용자)

#### 이미 구현됨
- 부서 CRUD + 검색/필터/정렬/페이징
- 사용자 CRUD/상태변경/검색/필터/정렬/페이징
- 부서/사용자 엑셀 업로드, 템플릿 다운로드, 업로드 결과/이력
- 실패 행/실패 사유/부분 성공 처리/실패 CSV 다운로드

#### 보완 필요
- 대량 업로드 성능 튜닝 지표(운영 모니터링 문서 보강)

#### 미구현
- 없음(A 기준)

### 2.3 평가 항목/템플릿 관리

#### 이미 구현됨
- 템플릿 CRUD, 문항 CRUD
- 문항 유형/필수/척도 관련 입력 처리
- 평가 문항 엑셀 업로드/중복 및 유효성 오류 처리
- 템플릿 목록 검색/필터/정렬/페이징

#### 보완 필요
- 템플릿 묶음 고급 버전관리(복제 고도화)는 B 이상

#### 미구현
- 없음(A 기준)

### 2.4 평가 세션/관계/배정 관리

#### 이미 구현됨
- 세션 생성/수정/삭제/목록(검색/필터/정렬/페이징)
- 세션 시작/종료/복제
- 자동 관계 생성 + 수동 관계 추가/삭제
- 관계 목록 검색/필터/정렬/페이징
- 세션 상세 배정표 검색/필터/정렬/페이징

#### 보완 필요
- 관계 자동 생성 규칙 고급 옵션 UX

#### 미구현
- 없음(A 기준)

### 2.5 일반 사용자 평가 제출

#### 이미 구현됨
- 로그인/내 할당 목록/점수·의견 입력/제출
- 제출 상태 반영 및 세션 정책 기반 제출 제어

#### 보완 필요
- 임시저장 UX 고도화 여부는 B 이상 검토

#### 미구현
- 없음(A 기준)

### 2.6 결과 조회/대시보드

#### 이미 구현됨
- 결과 조회 기본형(세션/대상자/부서/제출률/미제출자)
- 결과 CSV(대상자별/부서별/미제출자) 다운로드
- 기관 관리자 대시보드 운영 지표 + 최근 작업/바로가기
- 슈퍼 관리자 대시보드 기관 운영 요약

#### 보완 필요
- 고급 통계/시각화는 B/C 범위

#### 미구현
- 없음(A 기준)

### 2.7 감사 로그/운영 추적/권한 보안

#### 이미 구현됨
- 감사 로그 조회(검색/필터/정렬/페이징) + CSV export
- 업로드 실패 로그/오류 추적
- ROLE 경계(슈퍼/기관/사용자) 및 기관 데이터 격리
- 직접 URL 접근 제어, 타 기관 접근 차단

#### 보완 필요
- 운영 감사 항목 커스터마이징(보고서 템플릿화)

#### 미구현
- 없음(A 기준)

## 3. v1.0 필수 범위(A) 누락 기능 목록

- 기능 누락 관점에서는 현재 치명 누락 없음.
- 남은 작업은 “기능 추가”가 아니라 “운영 문서/정책 정리 + 경계 테스트 보강” 성격.

## 4. 구현 우선순위 로드맵 (A 종료 후)

1. A 종료 정리
   - 정렬/필터 기본 정책 문서화
   - A 인벤토리 확정 및 운영 체크리스트 동기화
2. B 범위 진입
   - 세션 복제 고도화
   - 결과 다운로드 포맷 확장
   - 관리자 작업 이력 강화
   - 대시보드 UX 개선

## 5. B 범위 진행 상태 (2026-03-12 업데이트)

- 세션 복제 고도화: 완료
- 결과 다운로드 포맷 확장: 완료
- 관리자 작업 이력 강화: 완료
- 대시보드 UX 개선: 완료

상세는 [V1_B_ENHANCEMENTS_STATUS.md](./V1_B_ENHANCEMENTS_STATUS.md) 참고.

## 6. 테스트 커버리지 상태 (요약)

- 시나리오 기반 통합 테스트:
  - `SecurityAndAdminScenarioIntegrationTest`
  - `EvaluationWorkflowScenarioIntegrationTest`
  - `StagingReadinessRehearsalIntegrationTest`
- 최근 반영 영역:
  - 기관/부서/템플릿/업로드/감사로그/세션상세 배정의 정렬·페이징 케이스
